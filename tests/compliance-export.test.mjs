import assert from "node:assert/strict";import {createRequire} from "node:module";const require=createRequire(import.meta.url);const ExcelJS=require("../vendor/exceljs.min.js");
const wb=new ExcelJS.Workbook();for(const name of ["企业画像与使用说明","法规识别台账","符合性评价记录","法规动态跟踪及官方来源"]){const ws=wb.addWorksheet(name);ws.addRow(["法规名称","官方来源"]);ws.addRow(["安全生产法",{text:"国家法律法规数据库",hyperlink:"https://flk.npc.gov.cn/"}]);ws.views=[{state:"frozen",ySplit:1}];ws.autoFilter={from:"A1",to:"B2"};ws.getRow(1).eachCell(c=>{c.font={bold:true,color:{argb:"FFFFFFFF"}};c.fill={type:"pattern",pattern:"solid",fgColor:{argb:"FF1E3A5F"}};});}
const buf=await wb.xlsx.writeBuffer();assert.ok(buf.length>5000);assert.equal(Buffer.from(buf).subarray(0,2).toString(),"PK");const reread=new ExcelJS.Workbook();await reread.xlsx.load(buf);assert.deepEqual(reread.worksheets.map(s=>s.name),["企业画像与使用说明","法规识别台账","符合性评价记录","法规动态跟踪及官方来源"]);assert.equal(reread.getWorksheet("法规识别台账").getCell("B2").value.hyperlink,"https://flk.npc.gov.cn/");console.log(JSON.stringify({status:"PASS",bytes:buf.length,sheets:reread.worksheets.length}));
