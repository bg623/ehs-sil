import assert from "node:assert/strict";import {createRequire} from "node:module";const require=createRequire(import.meta.url),Workflow=require("../js/compliance-workflow.js"),Engine=require("../js/compliance-engine.js");
const status=e=>Workflow.evaluationStatus(e,"2026-08-01",Engine.daysBetween);
assert.equal(status({judgment:"待评价"}),"未开始");assert.equal(status({judgment:"不符合",due:"2026-07-20"}),"已逾期 12 天");assert.equal(status({judgment:"基本符合",completedAt:"2026-07-31"}),"待验证");assert.equal(status({judgment:"基本符合",completedAt:"2026-07-31",verification:"验证通过"}),"已关闭");assert.deepEqual(Workflow.requiredMissing({judgment:"不符合"}),["差距分析","整改措施","责任人","计划完成日期"]);
const project=Workflow.createProject({profile:{companyName:"测试企业"},selected:{A:true,B:false},evaluations:{A:{}},addedUpdates:["AQ 3067-2026"],updatedAt:"2026-08-01T00:00:00.000Z"});assert.equal(Workflow.validateProject(project),project);assert.deepEqual(project.selectedRegulationIds,["A"]);assert.throws(()=>Workflow.validateProject({...project,schemaVersion:"9"}),/不支持的项目版本/);assert.throws(()=>Workflow.validateProject({...project,evaluations:null}),/缺少评价记录/);console.log(JSON.stringify({status:"PASS",schemaVersion:project.schemaVersion,statuses:4}));
